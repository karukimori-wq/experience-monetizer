'use client';
import {useEffect,useMemo,useState} from 'react';
import {betaCatalog,catalogMeta,JobCategory} from '@/data/jobCatalog';
const categories=['すべて',...Array.from(new Set(betaCatalog.map(j=>j.category)))] as ('すべて'|JobCategory)[];
const STORE='experience-monetizer-favorites';
export default function JobsPage(){const [query,setQuery]=useState('');const [category,setCategory]=useState<'すべて'|JobCategory>('すべて');const [hours,setHours]=useState(10);const [load,setLoad]=useState(5);const [favorites,setFavorites]=useState<string[]>([]);useEffect(()=>{const raw=localStorage.getItem(STORE);if(raw)setFavorites(JSON.parse(raw));},[]);function toggle(id:string){setFavorites(prev=>{const next=prev.includes(id)?prev.filter(v=>v!==id):[...prev,id].slice(-4);localStorage.setItem(STORE,JSON.stringify(next));return next})}const items=useMemo(()=>betaCatalog.filter(j=>(category==='すべて'||j.category===category)&&j.weeklyHours<=hours&&j.startupLoad<=load&&(!query||`${j.name} ${j.keywords.join(' ')}`.toLowerCase().includes(query.toLowerCase()))),[query,category,hours,load]);return <main className="results-shell"><header className="catalog-header"><div><span className="eyebrow">SIDE JOB CATALOG</span><h1>副業を探す</h1><p>250件の候補を条件から探し、気になる副業を最大4件まで比較できます。</p></div><div className="catalog-actions"><a href="/results" className="nav-cta">診断結果 →</a><a href="/compare" className="primary-button link-button">比較する ({favorites.length})</a></div></header><section className="dashboard-card catalog-filters"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="副業名・スキルで検索"/><div className="filter-row"><label>カテゴリー<select value={category} onChange={e=>setCategory(e.target.value as typeof category)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>週の時間 上限<select value={hours} onChange={e=>setHours(Number(e.target.value))}><option value="2">2時間</option><option value="5">5時間</option><option value="7">7時間</option><option value="10">10時間</option></select></label><label>初期負担 上限<select value={load} onChange={e=>setLoad(Number(e.target.value))}><option value="1">とても低い</option><option value="2">低い</option><option value="3">普通</option><option value="5">すべて</option></select></label></div></section><div className="catalog-summary"><strong>{items.length}</strong>件表示 <span>／ {catalogMeta.currentSeedCount}件収録</span></div><section className="catalog-grid">{items.map(j=><article className="catalog-card" key={j.id}><div className="catalog-tags"><span>{j.category}</span><span>{j.revenueModel}</span></div><h2>{j.name}</h2><div className="catalog-stats"><span>週 {j.weeklyHours}h〜</span><span>初期負担 {j.startupLoad}/5</span><span>難易度 {j.difficulty}/5</span></div><p>収益目安 ¥{j.incomeMin.toLocaleString()}〜¥{j.incomeMax.toLocaleString()}/月</p><div className="keyword-row">{j.keywords.map(k=><small key={k}>{k}</small>)}</div><div className="card-actions"><a className="detail-button" href={`/jobs/${j.id}`}>詳しく見る</a><button className={favorites.includes(j.id)?'favorite-button active':'favorite-button'} onClick={()=>toggle(j.id)}>{favorites.includes(j.id)?'✓ 比較中':'+ 比較'}</button></div></article>)}</section></main>}
