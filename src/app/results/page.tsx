'use client';
import {useEffect,useState} from 'react';
import {Answers} from '@/data/sideJobs';
import {calculateCatalogMatches,CatalogMatch} from '@/data/catalogMatcher';
export default function ResultsPage(){const [results,setResults]=useState<CatalogMatch[]>([]);const [confidence,setConfidence]=useState(0);const [skills,setSkills]=useState<Record<string,number>>({});useEffect(()=>{const raw=localStorage.getItem('experience-monetizer-answers');if(!raw)return;const answers:Answers=JSON.parse(raw);const deepRaw=localStorage.getItem('experience-monetizer-skills');const deep:Record<string,number>=deepRaw?JSON.parse(deepRaw):{};setSkills(deep);const answered=Object.keys(answers).filter(k=>answers[k]?.length).length;setConfidence(Math.min(92,25+answered*4+Object.keys(deep).length*7));setResults(calculateCatalogMatches(answers,deep));},[]);const top=results.slice(0,10);return <main className="results-shell"><section className="results-hero"><span className="eyebrow">YOUR MATCHES</span><h1>250の選択肢から、<br/>あなた向けの副業を選びました。</h1><p>仕事・経験・趣味・使える時間・環境・避けたい条件を組み合わせ、ベータ版の全副業カタログからランキングしています。</p><div className="result-metrics"><div><strong>{results.length||'—'}</strong><span>診断対象</span></div><div><strong>{confidence||'—'}{confidence?'%':''}</strong><span>診断信頼度</span></div><div><strong>{Object.keys(skills).length}</strong><span>確認済みスキル</span></div></div></section>{top.length?<><section className="result-list">{top.map((item,index)=><article className="result-card" key={item.id}><div className="result-rank">{String(index+1).padStart(2,'0')}</div><div className="result-main"><span className="status-pill">{item.category} · {item.revenueModel}</span><h2>{item.name}</h2><p>{item.reason}</p><div className="mini-metrics"><span>マッチ度 <strong>{item.score}%</strong></span><span>開始準備度 <strong>{item.readiness}%</strong></span><span>週 <strong>{item.weeklyHours}h〜</strong></span><span>初期負担 <strong>{item.startupLoad}/5</strong></span></div></div><a className="detail-button" href={`/jobs/${item.id}`}>詳しく見る →</a></article>)}</section><div className="catalog-summary"><a href="/jobs" className="nav-cta">250件の副業カタログをすべて見る →</a></div></>:<section className="dashboard-card"><h2>診断データがありません</h2><a className="primary-button link-button" href="/diagnosis">診断を始める</a></section>}<section className="deepen-card"><div><span className="eyebrow">NEXT STEP</span><h2>診断精度をさらに上げる</h2><p>上位候補に関係するスキルを深掘りすると、250件の順位を再計算します。</p></div><a href="/deep-diagnosis" className="primary-button link-button">診断を深める</a></section></main>}
