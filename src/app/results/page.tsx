'use client';
import {useEffect,useState} from 'react';
import {Answers} from '@/data/sideJobs';
import {calculateCatalogMatches,CatalogMatch} from '@/data/catalogMatcher';
import {getCatalogSkills} from '@/data/catalogDetail';
const FAV='experience-monetizer-favorites';
function insight(item:CatalogMatch,skills:Record<string,number>){const required=getCatalogSkills(item);const scored=required.map(s=>({...s,current:skills[s.name]??0}));const known=scored.filter(s=>s.current>0).sort((a,b)=>(b.current-b.required)-(a.current-a.required));const gaps=scored.filter(s=>s.current<s.required).sort((a,b)=>(b.required-b.current)-(a.required-a.current));const strengths=known.slice(0,2).map(s=>s.name);if(!strengths.length)strengths.push(...item.matchedKeywords.slice(0,2));return{strengths,gaps:gaps.slice(0,2).map(s=>s.name)}}
export default function ResultsPage(){const [results,setResults]=useState<CatalogMatch[]>([]);const [confidence,setConfidence]=useState(0);const [skills,setSkills]=useState<Record<string,number>>({});const [favorites,setFavorites]=useState<string[]>([]);useEffect(()=>{const raw=localStorage.getItem('experience-monetizer-answers');if(!raw)return;const answers:Answers=JSON.parse(raw);const deepRaw=localStorage.getItem('experience-monetizer-skills');const deep:Record<string,number>=deepRaw?JSON.parse(deepRaw):{};setSkills(deep);setFavorites(JSON.parse(localStorage.getItem(FAV)||'[]'));const answered=Object.keys(answers).filter(k=>answers[k]?.length).length;setConfidence(Math.min(92,25+answered*4+Object.keys(deep).length*7));setResults(calculateCatalogMatches(answers,deep));},[]);function toggle(id:string){setFavorites(prev=>{const next=prev.includes(id)?prev.filter(v=>v!==id):[...prev,id].slice(-4);localStorage.setItem(FAV,JSON.stringify(next));return next})}const top=results.slice(0,10);return <main className="results-shell"><section className="results-hero results-hero-compact"><span className="eyebrow">YOUR MATCHES</span><h1>あなたに合う副業を<br/>見つけました。</h1><p>経験・使える時間・働き方・避けたい条件から、250件の副業をあなた向けに並べています。</p><div className="result-overview"><div className="result-overview-main"><span>診断信頼度</span><strong>{confidence||'—'}{confidence?'%':''}</strong><small>確認済みスキル {Object.keys(skills).length}件</small></div><div className="result-overview-sub"><span>ランキング対象</span><strong>{results.length||'—'}</strong><small>副業候補</small></div></div><div className="results-guide"><strong>まずはTOP3を見てください</strong><span>気になる候補は比較に追加すると、収入・時間・難易度を横並びで確認できます。</span></div></section>{top.length?<><section className="result-list">{top.map((item,index)=>{const info=insight(item,skills);const selected=favorites.includes(item.id);return <article className={index<3?'result-card result-card-featured':'result-card'} key={item.id}><div className="result-rank-wrap"><span className="result-rank-label">RANK</span><div className="result-rank">{String(index+1).padStart(2,'0')}</div></div><div className="result-main"><div className="result-card-top"><span className="status-pill">{item.category} · {item.revenueModel}</span><span className="result-score-chip">{item.score}%</span></div><h2>{item.name}</h2><p>{item.reason}</p><div className="result-quick-facts"><span><small>準備度</small><strong>{item.readiness}%</strong></span><span><small>週の目安</small><strong>{item.weeklyHours}h〜</strong></span><span><small>初期負担</small><strong>{item.startupLoad}/5</strong></span></div><div className="result-insights"><div><small>活かせる強み</small><strong>{info.strengths.length?info.strengths.join('・'):'診断を深めると表示されます'}</strong></div><div><small>伸ばすと有利</small><strong>{info.gaps.length?info.gaps.join('・'):'大きな不足はありません'}</strong></div></div></div><div className="result-actions"><a className="detail-button result-primary-action" href={`/jobs/${item.id}`}>この副業を見る →</a><button className={selected?'favorite-button active':'favorite-button'} onClick={()=>toggle(item.id)}>{selected?'✓ 比較中':'+ 比較に追加'}</button></div></article>})}</section><div className="catalog-summary result-summary-actions"><a href="/jobs" className="nav-cta">250件の副業をすべて見る →</a>{favorites.length>1&&<a href="/compare" className="primary-button link-button result-compare-cta">選んだ{favorites.length}件を比較する</a>}</div></>:<section className="dashboard-card"><h2>診断データがありません</h2><a className="primary-button link-button" href="/diagnosis">診断を始める</a></section>}<section className="deepen-card"><div><span className="eyebrow">NEXT STEP</span><h2>診断精度をさらに上げる</h2><p>上位候補に関係するスキルを深掘りすると、250件の順位を再計算します。</p></div><a href="/deep-diagnosis" className="primary-button link-button">診断を深める</a></section></main>}