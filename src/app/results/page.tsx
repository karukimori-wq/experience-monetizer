'use client';
import {useEffect,useState} from 'react';
import {Answers} from '@/data/sideJobs';
import {calculateCatalogMatches,CatalogMatch} from '@/data/catalogMatcher';
import {getCatalogSkills} from '@/data/catalogDetail';
const FAV='experience-monetizer-favorites';
function insight(item:CatalogMatch,skills:Record<string,number>){const required=getCatalogSkills(item);const scored=required.map(s=>({...s,current:skills[s.name]??0}));const known=scored.filter(s=>s.current>0).sort((a,b)=>(b.current-b.required)-(a.current-a.required));const gaps=scored.filter(s=>s.current<s.required).sort((a,b)=>(b.required-b.current)-(a.required-a.current));const strengths=known.slice(0,2).map(s=>s.name);if(!strengths.length)strengths.push(...item.matchedKeywords.slice(0,2));return{strengths,gaps:gaps.slice(0,2).map(s=>s.name)}}
export default function ResultsPage(){const [results,setResults]=useState<CatalogMatch[]>([]);const [confidence,setConfidence]=useState(0);const [skills,setSkills]=useState<Record<string,number>>({});const [favorites,setFavorites]=useState<string[]>([]);useEffect(()=>{const raw=localStorage.getItem('experience-monetizer-answers');if(!raw)return;const answers:Answers=JSON.parse(raw);const deepRaw=localStorage.getItem('experience-monetizer-skills');const deep:Record<string,number>=deepRaw?JSON.parse(deepRaw):{};setSkills(deep);setFavorites(JSON.parse(localStorage.getItem(FAV)||'[]'));const answered=Object.keys(answers).filter(k=>answers[k]?.length).length;setConfidence(Math.min(92,25+answered*4+Object.keys(deep).length*7));setResults(calculateCatalogMatches(answers,deep));},[]);function toggle(id:string){setFavorites(prev=>{const next=prev.includes(id)?prev.filter(v=>v!==id):[...prev,id].slice(-4);localStorage.setItem(FAV,JSON.stringify(next));return next})}const top=results.slice(0,10);return <main className="results-shell"><section className="results-hero"><span className="eyebrow">YOUR MATCHES</span><h1>250の選択肢から、<br/>あなた向けの副業を選びました。</h1><p>仕事・経験・趣味・使える時間・環境・避けたい条件を組み合わせ、ベータ版の全副業カタログからランキングしています。</p><div className="result-metrics"><div><strong>{results.length||'—'}</strong><span>診断対象</span></div><div><strong>{confidence||'—'}{confidence?'%':''}</strong><span>診断信頼度</span></div><div><strong>{Object.keys(skills).length}</strong><span>確認済みスキル</span></div></div></section>{top.length?<><section className="result-list">{top.map((item,index)=>{const info=insight(item,skills);const selected=favorites.includes(item.id);return <article className="result-card" key={item.id}><div className="result-rank">{String(index+1).padStart(2,'0')}</div><div className="result-main"><span className="status-pill">{item.category} · {item.revenueModel}</span><h2>{item.name}</h2><p>{item.reason}</p><div className="result-insights"><div><small>活かせる強み</small><strong>{info.strengths.length?info.strengths.join('・'):'診断を深めると表示されます'}</strong></div><div><small>伸ばすと有利</small><strong>{info.gaps.length?info.gaps.join('・'):'大きな不足はありません'}</strong></div></div><div className="mini-metrics"><span>マッチ度 <strong>{item.score}%</strong></span><span>開始準備度 <strong>{item.readiness}%</strong></span><span>週 <strong>{item.weeklyHours}h〜</strong></span><span>初期負担 <strong>{item.startupLoad}/5</strong></span></div></div><div className="result-actions"><button className={selected?'favorite-button active':'favorite-button'} onClick={()=>toggle(item.id)}>{selected?'✓ 比較中':'+ 比較'}</button><a className="detail-button" href={`/jobs/${item.id}`}>詳しく見る →</a></div></article>})}</section><div className="catalog-summary result-summary-actions"><a href="/jobs" className="nav-cta">250件の副業カタログをすべて見る →</a>{favorites.length>1&&<a href="/compare" className="primary-button link-button">選んだ{favorites.length}件を比較する</a>}</div></>:<section className="dashboard-card"><h2>診断データがありません</h2><a className="primary-button link-button" href="/diagnosis">診断を始める</a></section>}<section className="deepen-card"><div><span className="eyebrow">NEXT STEP</span><h2>診断精度をさらに上げる</h2><p>上位候補に関係するスキルを深掘りすると、250件の順位を再計算します。</p></div><a href="/deep-diagnosis" className="primary-button link-button">診断を深める</a></section></main>}