'use client';
import {FormEvent,useEffect,useMemo,useState} from 'react';import {betaCatalog} from '@/data/jobCatalog';
type Log={id:number;date:string;hours:number;sales:number;cost:number;memo:string};const KEY='experience-monetizer-activity';
export default function ActivityPage(){const [jobId,setJobId]=useState('');const [logs,setLogs]=useState<Log[]>([]);const [date,setDate]=useState('');const [hours,setHours]=useState('');const [sales,setSales]=useState('');const [cost,setCost]=useState('');const [memo,setMemo]=useState('');useEffect(()=>{setJobId(localStorage.getItem('experience-monetizer-active-job')||'');setLogs(JSON.parse(localStorage.getItem(KEY)||'[]'));setDate(new Date().toISOString().slice(0,10))},[]);const job=betaCatalog.find(j=>j.id===jobId);const totals=useMemo(()=>logs.reduce((a,l)=>({hours:a.hours+l.hours,sales:a.sales+l.sales,cost:a.cost+l.cost}),{hours:0,sales:0,cost:0}),[logs]);const profit=totals.sales-totals.cost;const hourly=totals.hours?Math.round(profit/totals.hours):0;function add(e:FormEvent){e.preventDefault();const next=[{id:Date.now(),date,hours:Number(hours)||0,sales:Number(sales)||0,cost:Number(cost)||0,memo},...logs];setLogs(next);localStorage.setItem(KEY,JSON.stringify(next));setHours('');setSales('');setCost('');setMemo('')}function remove(id:number){const next=logs.filter(l=>l.id!==id);setLogs(next);localStorage.setItem(KEY,JSON.stringify(next))}return <main className="results-shell"><header className="catalog-header"><div><span className="eyebrow">ACTIVITY TRACKER</span><h1>{job?job.name:'副業'}の実績</h1><p>使った時間とお金、得られた売上を記録して「本当に自分に合う副業か」を数字で判断します。</p></div><a href="/start-plan" className="nav-cta">← スタートプラン</a></header><section className="activity-metrics"><div><span>累計売上</span><strong>¥{totals.sales.toLocaleString()}</strong></div><div><span>累計経費</span><strong>¥{totals.cost.toLocaleString()}</strong></div><div><span>利益</span><strong>¥{profit.toLocaleString()}</strong></div><div><span>作業時間</span><strong>{totals.hours}h</strong></div><div><span>実質時給</span><strong>¥{hourly.toLocaleString()}</strong></div></section><section className="activity-layout"><form className="dashboard-card activity-form" onSubmit={add}><span className="eyebrow">ADD RECORD</span><h2>活動を記録</h2><label>日付<input type="date" value={date} onChange={e=>setDate(e.target.value)} required/></label><label>作業時間（時間）<input type="number" min="0" step="0.5" value={hours} onChange={e=>setHours(e.target.value)} required/></label><label>売上（円）<input type="number" min="0" value={sales} onChange={e=>setSales(e.target.value)}/></label><label>経費（円）<input type="number" min="0" value={cost} onChange={e=>setCost(e.target.value)}/></label><label>メモ<input value={memo} onChange={e=>setMemo(e.target.value)} placeholder="例：初めて商品が売れた"/></label><button className="primary-button">記録する</button></form><section className="dashboard-card"><span className="eyebrow">HISTORY</span><h2>活動履歴</h2><div className="activity-list">{logs.length?logs.map(l=><article key={l.id}><div><strong>{l.date}</strong><span>{l.hours}h · 売上 ¥{l.sales.toLocaleString()} · 経費 ¥{l.cost.toLocaleString()}</span>{l.memo&&<small>{l.memo}</small>}</div><button onClick={()=>remove(l.id)}>×</button></article>):<p className="muted-copy">まだ記録がありません。最初の活動から記録してみましょう。</p>}</div></section></section><section className="deepen-card"><div><span className="eyebrow">REVIEW</span><h2>{profit>0?'収益が生まれています。次は再現性を確認しましょう。':totals.hours>0?'まずは小さな売上につながる行動を確認しましょう。':'最初の一歩を記録すると、判断材料が増えていきます。'}</h2><p>売上だけでなく、時間と経費を一緒に見ることで、続ける・改善する・別の副業を試す判断がしやすくなります。</p></div><a href="/jobs" className="primary-button link-button">他の副業も見る</a></section></main>}
